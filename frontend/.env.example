# Public backend origin for production builds or when not using Vite dev proxy.
# Leave unset in local dev to use same-origin `/api/v1` (see vite.config.ts proxy).
# VITE_API_BASE_URL=http://localhost:8000
