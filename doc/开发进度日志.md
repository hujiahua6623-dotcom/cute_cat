## 开发进度日志（cute_cat）

用于按天记录项目推进情况，方便回顾与协作同步。

---

## 2026-03-23（修复：双端指针不同步 + 视觉可见更新）

### 今日完成

1. 修复前端花园 ID 来源问题：
   - [`frontend/src/network/wsClient.ts`](../frontend/src/network/wsClient.ts) `connect()` 改为返回 `ws-ticket` 结果；
   - [`frontend/src/main.ts`](../frontend/src/main.ts) 改为使用 ticket 返回的 `gardenId` 进行 `joinGarden`、`updatePointer`、`petAction`，避免旧账号因 `/me` 与 ticket 花园 ID 不一致导致同步失效。
2. 视觉可见更新：
   - [`frontend/src/game/GardenScene.ts`](../frontend/src/game/GardenScene.ts) 增强场景纹理（天空、树、房屋、小路、花朵）与宠物阴影；
   - [`frontend/src/styles/app.css`](../frontend/src/styles/app.css) 增强页面背景层次（蓝天 + 草地渐变），主画面不再纯色平铺。
3. 验证：
   - `npm run build` 通过；
   - 截图脚本 `node scripts/capture-screenshots.mjs` 通过（`wsJoinPassed=true`、`actionDeltaPassed=true`），并刷新 `artifacts/screenshots/*.png`。

### 当前状态

- 指针同步链路已按后端共享花园逻辑对齐（以 ws-ticket gardenId 为准）。
- 视觉相较上一版有明显变化，但与 `assets/ui/` 的高保真像素稿仍有差距（属于下一轮美术精修范围）。

### 下一步建议

1. 先请测试同学用“两账号同花园”实测鼠标同步，确认线上复现问题已消失。
2. 将登录/领宠面板继续做像素风装饰层（边角、图标、按钮高光）以缩小与参考稿差距。

---

## 2026-03-23（周期 1A 收口评审：视觉差异清单 + 进入周期 2 决策）

### 人工走查范围

- 参考稿：`assets/ui/ui_garden_main.png`、`ui_auth_login.png`、`ui_pet_claim.png`、`ui_offline_summary.png`
- 实际截图：`artifacts/screenshots/frontend-login.png`、`frontend-claim.png`、`frontend-garden.png`、`frontend-offline-toast.png`

### 视觉差异结论

1. **主结构一致（通过）**：登录、领宠、花园、离线摘要四个核心页面均已具备对应布局与可用交互，不再是纯功能占位空白页。
2. **风格仍有差距（待精修）**：与参考稿相比，当前版本在背景层次、装饰元素、像素细节、按钮立体感与图标语义（如状态图标）上仍偏简化。
3. **花园页差距最大（可接受）**：当前花园场景为可玩样式，但未达到 `ui_garden_main` 的完整美术表现（花园景深、房屋细节、底部整合式 HUD 造型仍缺）。
4. **联调链路无回退（通过）**：自动脚本结果保持 `wsJoinPassed=true`、`actionDeltaPassed=true`，离线摘要弹层可稳定出现并关闭。

### 决策

- **周期 1A 判定通过（可收口）**：满足“功能不回退 + 可演示视觉基线”的目标。
- **允许进入周期 2**（经济/生病/成长），同时将视觉精修项作为并行 backlog（不阻塞业务推进）。

### 周期 2 前置建议（并行，不阻塞）

1. 追加一轮美术精修任务：优先花园底图、宠物精灵、底部 HUD 一体化样式。
2. 保持现有组件边界（`ui/views`、`ui/gardenHud`、`ui/wsHandlers`）不回退，避免周期 2 再次堆回 `main.ts`。

---

## 2026-03-23（前端：周期 1A 继续推进，消息处理拆分 + 截图回归）

### 今日完成

1. 新增 [`frontend/src/ui/wsHandlers.ts`](../frontend/src/ui/wsHandlers.ts)，将 WebSocket 消息分发与场景更新逻辑从 [`frontend/src/main.ts`](../frontend/src/main.ts) 中抽离为 `createWsMessageHandler`。
2. 更新 [`frontend/src/main.ts`](../frontend/src/main.ts)，保留连接编排与事件绑定，消息处理改为模块化依赖注入（`store`、`scene`、`syncHud`）。
3. 验证：`npm run build` 通过；`ReadLints` 对改动文件无新增错误。
4. 执行截图脚本 [`frontend/scripts/capture-screenshots.mjs`](../frontend/scripts/capture-screenshots.mjs) 成功，输出刷新到 `artifacts/screenshots/`：
   - `frontend-login.png`
   - `frontend-claim.png`
   - `frontend-garden.png`
   - `frontend-offline-toast.png`
5. 自动验收结果（脚本输出）：`wsJoinPassed=true`、`actionDeltaPassed=true`、`usedOfflineModalForFourthShot=true`。

### 当前状态

- 周期 1A 的 T6 持续推进：主流程、视图、HUD、消息处理均已分层，`main.ts` 复杂度进一步下降。
- 视觉回归截图与关键链路验证结果已留痕，可作为 1A 阶段验收证据的一部分。

### 下一步建议

1. 做一次人工走查（对照 `assets/ui/ui_garden_main.png` 与 `ui_auth_*`）确认细节差异清单。
2. 若无新增视觉阻塞，准备 1A 收口结论并切换进入周期 2（经济/生病/成长）。

---

## 2026-03-23（前端：周期 1A 继续推进，HUD/连接态组件化）

### 今日完成

1. 新增 [`frontend/src/ui/gardenHud.ts`](../frontend/src/ui/gardenHud.ts)，抽离花园 HUD 渲染与连接状态条渲染（`renderGardenHud`、`renderWsStatusBar`）。
2. 更新 [`frontend/src/main.ts`](../frontend/src/main.ts)，移除内联 HUD 与 WS 状态文案拼接逻辑，主流程仅保留数据流与事件编排。
3. 验证：`npm run build` 通过；`ReadLints` 对改动文件无新增错误。

### 当前状态

- 周期 1A 的 T6 组件化进一步推进：认证/领宠/花园视图构建与 HUD 渲染已分层，`main.ts` 复杂度继续下降。
- 功能链路保持不变，未改动后端协议与 WebSocket 消息结构。

### 下一步建议

1. 继续拆分 `actionButtons` 事件绑定与 `handleServerMessage` 分发（可抽 `ui/gardenRuntime.ts`）。
2. 完成一轮截图回归并在日志补充“视觉 + 无回退”验收证据。
3. 1A 收口后进入周期 2（经济/生病/成长）。

---

## 2026-03-23（前端：周期 1A 继续推进，完成 T6 主文件拆分）

### 今日完成

1. 新增 [`frontend/src/ui/views.ts`](../frontend/src/ui/views.ts)，抽离认证页、领宠页、花园 HUD 视图构建逻辑，减少 [`frontend/src/main.ts`](../frontend/src/main.ts) 模板拼接与 DOM 查询密度。
2. 重构 [`frontend/src/main.ts`](../frontend/src/main.ts)：保留鉴权、API、WebSocket 与状态流转逻辑，将页面结构改为复用 `createAuthView/createClaimView/createGardenView`。
3. 补充样式细节 [`frontend/src/styles/app.css`](../frontend/src/styles/app.css)：`section-title`、`hud-foot` 等辅助类，去除内联样式依赖。
4. 验证：`npm run build` 通过；`ReadLints` 对改动文件无新增错误。

### 当前状态

- 周期 1A 的 T6（主文件拆分）从“部分完成”推进为“可用版本”，后续可以继续按组件粒度拆分动作条/状态条/连接态。
- 周期 1 功能链路未变，仍保持可联调。

### 下一步建议

1. 继续执行周期 1A：将 HUD 子块拆到 `src/ui/*`（`StatusBars`、`ActionBar`、`WsStatusBar`），提升可维护性。
2. 按相同交互流程重拍一组前端截图（登录、领宠、花园、离线摘要），作为 1A 验收附件。
3. 视觉冲刺收口后，进入周期 2 业务开发（经济/生病/成长）。

---

## 2026-03-23（项目管理：周期 1 完成口径 + 周期 1A 启动）

### 今日完成

1. 对齐并确认“周期 1 已完成（功能口径）”：`joinGarden -> petAction -> petStateDelta`、离线摘要、双端同步等核心验收能力保留为通过状态，不因视觉问题回退结论。
2. 在 [doc/开发周期计划.md](开发周期计划.md) 新增 **周期 1A：视觉整合冲刺（功能不回退）**，补充目标、主要工作、验收标准与时间盒（0.5-1 周）。
3. 将路线图主链调整为 `周期1 -> 周期1A -> 周期2`，明确先固化视觉基线再进入经济/生病/成长开发，降低后续返工。

### 当前状态

- 周期 1：按功能验收已完成。
- 周期 1A：已立项并进入执行，目标是将当前可玩占位 UI 提升为与 `assets/ui/` 对齐的可演示版本。

### 下一步建议

1. 前端按 [doc/前端开发任务与对接.md](前端开发任务与对接.md) §9A 执行 T1-T5，T6 视时间执行。
2. 完成视觉整合后立刻做“周期 1 链路无回退”回归，并在本日志补充截图与验收结论。
3. 周期 1A 验收通过后，进入周期 2（经济/生病/成长）功能开发。

---

## 2026-03-23（前端：视觉整合冲刺派工清单）

### 今日完成

1. 在 [doc/前端开发任务与对接.md](前端开发任务与对接.md) 新增 **§9A 视觉整合冲刺（按文件路径派工）**，补充可直接指派的任务单（T1-T6）。
2. 明确本轮仅前端改动范围（`main.ts`、`GardenScene.ts`、`tokens.css`、`app.css`）与可选拆分路径（`src/ui/*`）。
3. 补充执行顺序（D1-D4）与联调不回退检查项，确保视觉升级不影响周期 1 已打通链路。

### 当前状态

- 前端开发可按文档直接开工视觉整合；任务优先级与验收口径已固定。
- 关键风险仍是“视觉升级导致联调回退”，已在 §9A.4 写入硬性回归项。

### 下一步建议

1. 前端按 T1/T2 优先完成主画面与 HUD。
2. 完成后立即执行 §9A.4 联调回归，再进入 T3/T4。

---

## 2026-03-22（前端：视觉整合冲刺 §9A）

### 今日完成

1. 完成 T1：[`frontend/src/game/GardenScene.ts`](../frontend/src/game/GardenScene.ts) 从纯色底 + 圆点改为纹理场景与宠物精灵占位，保留 `setPetNormPosition` 与 pointer 同步逻辑（坐标仍为 0~1）。
2. 完成 T2：[`frontend/src/styles/tokens.css`](../frontend/src/styles/tokens.css)、[`frontend/src/styles/app.css`](../frontend/src/styles/app.css) 统一 token 与组件样式，补齐按钮 `hover/disabled/loading`。
3. 完成 T3：[`frontend/src/main.ts`](../frontend/src/main.ts) 登录/注册/领宠界面升级为设计稿风格，错误态可视化（`error-banner`）。
4. 完成 T4：离线摘要弹层与动作 Toast 样式升级；新增 [`frontend/src/ui/feedback.ts`](../frontend/src/ui/feedback.ts) 抽离 `showError`、`showOfflineModal`、`showActionToast`（T6 模块化拆分的一部分）。
5. 完成 T5：连接顶栏支持 `connecting/connected/reconnecting/disconnected` 四态可见，断线不静默。
6. 回归验证：`npm run build` 通过；协议字段与后端接口未变更。

### 联调回归状态

- 注册 -> 领宠 -> 进花园：通过（前端链路保持可用）。
- `ws-ticket` -> `joinGarden`：前端逻辑保持不变，连接流程可触发。
- `petAction` -> `petStateDelta`：前端发送与更新逻辑保持可用。
- 双客户端指针/状态同步：需要同 `gardenId` 测试数据；后端当前默认一用户一花园，需后端/测试提供同花园联调条件。

### 阻塞与协作反馈

- 非协议问题：双客户端同步验收依赖“同花园双账号”测试前置，建议后端补测试种子或管理入口。

---

## 2026-03-22（前端：周期 0 + 周期 1 切片落地）

### 今日完成

1. 初始化 [`frontend/`](../frontend/)：`Vite` + `TypeScript` + `Phaser 3`，`vite.config.ts` 将 `/api` 代理至本机后端；[`frontend/.env.example`](../frontend/.env.example) 说明可选 `VITE_API_BASE_URL`。
2. 实现 [`frontend/src/network/httpClient.ts`](../frontend/src/network/httpClient.ts)（Bearer、`/auth/refresh` 重试）、[`wsClient.ts`](../frontend/src/network/wsClient.ts)（ticket 连接、断线后由上层换新 ticket 重连）、[`api.ts`](../frontend/src/network/api.ts) 与类型 [`types.ts`](../frontend/src/network/types.ts)。
3. 实现 [`gameStore.ts`](../frontend/src/state/gameStore.ts) 与 [`GardenScene.ts`](../frontend/src/game/GardenScene.ts)：归一化坐标、`updatePointer` 约 100ms 节流；`actionBroadcast` 对本人跳过动画（与对接文档 §7.2 一致）。
4. 用户旅程：登录/注册 → `GET /me` → 领宠 → `GET /pets/{id}` → 离线摘要弹窗 → `ws-ticket` → `joinGarden` → 三动作 `petAction`；HUD 状态条与 Toast（`petStateDelta`）。
5. 文档：更新根目录 [`README.md`](../README.md)、[`doc/项目目录说明.md`](项目目录说明.md)、[`.env.example`](../.env.example)、[`.gitignore`](../.gitignore)（跟踪 `frontend/.env.example`）；新增 [`frontend/README.md`](../frontend/README.md)。

### 联调注意

- 当前后端 `POST /pets/claim` 为**每用户新建独立花园**，双客户端同花园需测试数据或后续后端种子/分配策略（非前端单端问题）。
- 双开验收：两账号须共享 `gardenId` 方可互相看到指针与广播。

### 已知风险 / 后续

- 生产构建包体较大（Phaser）；可按需 `dynamic import` 拆分。
- 周期 2 可将 `main.ts` 中的叠层拆为 `components/` 模块。

### 会话交接

- **状态**：`npm run build` 已通过；本地需同时跑后端 + `npm run dev` 做完整联调。
- **下一动作**：与后端/测试确认同花园双用户数据；周期 2 接入商店/医院与成长面板深化。

---

## 2026-03-22（UI：§3 与 `assets/ui/` 对齐 + 反馈确认）

### 今日完成

1. **设计侧问题确认**：前端反馈工作区 `assets/ui/` 仅含部分 PNG、与 [doc/UI设计-规范与稿清单.md](UI设计-规范与稿清单.md) §3 不一致——**属实**，属清单中花园主界面、离线摘要等稿未落入仓库或分支未同步。
2. 补全缺失参考图至 `assets/ui/`：`ui_garden_main.png`、`ui_offline_summary.png`、`ui_action_result_toast.png`、`ui_pet_status_growth.png`、`ui_shop_hospital.png`、`ui_birthday_event.png`（与 §3 合计 **16** 个文件一致）。
3. 更新 [doc/UI设计-规范与稿清单.md](UI设计-规范与稿清单.md) §1（Figma 占位职责、仓库与 §10.3 对齐说明）、§3.1（16 文件名勾选列表）、修订记录。

### 非 UI 阻塞（产品 / 技术拍板）

- 见 [doc/前端开发任务与对接.md](前端开发任务与对接.md) **§11**：refresh 与 `localStorage` / 生产 HTTPS、多标签页同账号、移动端是否单独要稿——**仍为待确认项**；周期 1 移动稿默认不在「已补齐」范围。

### 工程侧提醒（非本次改动）

- `frontend/` 初始化、联调顺序见 [doc/前端开发任务与对接.md](前端开发任务与对接.md) §12；`actionBroadcast` 含本人、`pointerUpdate` 仅他人时动画 **§7.2 去重**——实现时按对接文档执行。

### 会话交接

- **状态**：`assets/ui/` 与 §3 / §3.1 清单已对齐；设计组仍须补 **§1 真实 Figma 链接**。
- **下一动作**：前端执行 §10.3 自检后开工；提交时确保 **16 个 PNG 一并进版本库**。

---

## 2026-03-21（UI：§10.2 参考稿与设计规范）

### 今日完成

1. 按 [doc/前端开发任务与对接.md](前端开发任务与对接.md) §10.2 补齐参考稿（像素风 mockup，命名 `assets/ui/ui_*.png`）：设计系统一页、登录/注册、Tab 同屏、认证错误态、领宠与 409 冲突、WebSocket 连接中/断开/重连顶栏叠层。
2. 新增 [doc/UI设计-规范与稿清单.md](UI设计-规范与稿清单.md)（色彩与字体 token、组件状态、全量稿清单、`error.code` 与表单字段对照）。
3. 更新 [doc/项目目录说明.md](项目目录说明.md)（`assets/ui/` 与 doc 索引）、[doc/前端开发任务与对接.md](前端开发任务与对接.md) §10.1 / §10.2。

### 变更文件 / 模块（摘要）

- `assets/ui/ui_design_system_components.png`、`ui_auth_login.png`、`ui_auth_register.png`、`ui_auth_login_register.png`、`ui_auth_error_states.png`、`ui_pet_claim.png`、`ui_pet_claim_conflict.png`、`ui_ws_connecting.png`、`ui_ws_disconnected.png`、`ui_ws_reconnecting.png`
- `doc/UI设计-规范与稿清单.md`
- `doc/项目目录说明.md`、`doc/前端开发任务与对接.md`

### 已知风险 / 注意

- 可编辑 Figma 链接在规范文档 §1 为占位，设计组需替换为实际文件地址。
- 移动端断点稿未出，与 §10.2「MVP 可仅桌面」一致。

### 下一步建议

1. 前端按规范文档实现 `pages` 鉴权流与领宠，并对照稿面做 HTML/CSS 叠层。
2. 设计组确认像素字体与 Figma 源是否与生成稿一致，必要时迭代一版矢量定稿。

### 会话交接

- **状态**：§10.2 仓库参考图与规范文档已齐；与既有 `ui_garden_main` 等系列稿同属 `assets/ui/`。
- **下一动作**：前端联调 `register/login` → `me` → `claim` → `ws-ticket` → `joinGarden`。

---

## 2026-03-21（后端：周期 0 + 周期 1 切片实现）

### 今日完成

1. 新增 `backend/` 工程：`FastAPI` + `SQLAlchemy` 2 异步（`asyncmy` / 测试用 `aiosqlite`）、`Alembic` 初版迁移（`users` / `gardens` / `pets` / `refresh_tokens`）。
2. 认证：`POST /api/v1/auth/register|login|refresh|logout`；access JWT；refresh 存 MySQL（无 Redis 亦可旋转）。
3. 业务：`GET /api/v1/me`，`POST /api/v1/pets/claim`，`GET /api/v1/pets/{petId}`（含被动衰减与简化 `stability`），`GET /api/v1/offline-summary`，`GET /api/v1/gardens/ws-ticket`。
4. 实时：`/api/v1/ws/garden?ticket=`，`joinGarden` / `updatePointer` / `petAction`（`Feed`/`Cuddle`/`Pat`），广播 `gardenSnapshot`、`pointerUpdate`、`actionBroadcast`、`petStateDelta`。
5. 自测：`pytest`（`tests/test_health.py`、`tests/test_auth_pets.py`）全部通过。
6. 文档：更新 [`doc/后端开发设计文档.md`](后端开发设计文档.md) §11、`doc/API-后端与前端对接.md`（健康检查双路径、可选 `itemId`、`petStateDelta` 约定、联调环境变量）、[`README.md`](../README.md)、[`doc/项目目录说明.md`](项目目录说明.md)；根目录 [`.env.example`](../.env.example) 增加 `PUBLIC_BASE_URL`、`WS_TICKET_TTL_SECONDS`。

### 变更文件 / 模块（摘要）

- `backend/src/cute_cat/`：应用入口、`api/routes/*`、`auth/*`、`game/*`、`persistence/*`、`realtime/*`、`services/pet_state.py`
- `backend/alembic/`：初始迁移 `001_initial.py`
- `backend/tests/`、`backend/README.md`、`backend/pyproject.toml`

### 已知风险 / 注意

- 多 worker 部署时 WebSocket 房间为进程内字典，需 Redis 或粘性会话；见 [`doc/后端开发设计文档.md`](后端开发设计文档.md) §11.6。
- `pip install -e .` 若遇本地 pip/setuptools 异常，可用 `PYTHONPATH=src` 运行 `uvicorn` / `pytest`（见 `backend/README.md`）。

### 下一步建议

1. 前端：按 [`doc/API-后端与前端对接.md`](API-后端与前端对接.md) 联调 HTTP + WebSocket。
2. 周期 2：商店/医院、完整成长滑动窗口、库存与扣费；必要时补充集成测试（真实 MySQL）。

### 会话交接

- **状态**：后端切片 0+2 最小闭环已可跑；数据库需执行 `alembic upgrade head`（MySQL）。
- **下一动作**：配置 `.env` 中 `DATABASE_URL` / `JWT_SECRET`，启动 `uvicorn`，用浏览器或 `wscat` 验证 WebSocket ticket 与 `joinGarden`。

---

## 2026-03-21

### 今日完成
1. 完成核心设计文档重构：`doc/项目设计文档.md`
   - 明确 MVP 边界（每用户 1 只宠物、多人同花园同步）
   - 明确时间换算与连续时钟（现实 1 小时 = 游戏 2 小时；离线不暂停）
   - 明确成长规则（情绪稳定优先，`sick_count` 0/1 口径）
   - 明确生日/社交活动时长与频率（生日固定 24 游戏小时；社交 1-2 周一次，持续 12-48 小时）
2. 新增并完善目录文档：`doc/项目目录说明.md`
   - 记录目录结构、资源用途、前后端分离建议
   - 记录确定技术栈（FastAPI + TypeScript/Phaser + 最新 LangChain）
3. 新增切片工程骨架文档：`doc/切片0-2工程骨架.md`
   - 细化切片 0（离线摘要）与切片 2（实时同步+动作反馈）的模块职责与验收清单
4. 生成并整理 UI 参考图到 `assets/ui/`
   - `ui_garden_main.png`
   - `ui_pet_status_growth.png`
   - `ui_shop_hospital.png`
   - `ui_birthday_event.png`
   - `ui_offline_summary.png`（已按反馈修正）
   - `ui_action_result_toast.png`
5. 完成开源基础文件
   - `README.md`
   - `LICENSE`（MIT）
   - `.gitignore`
   - `CONTRIBUTING.md`
   - `CODE_OF_CONDUCT.md`
   - `SECURITY.md`
   - `CHANGELOG.md`

### 当前状态
- 设计文档与开源基础文档齐备
- UI 参考图已可用于前端切片实现
- 下一步可进入“切片 0 + 2”的实际编码阶段

### 下一步建议
1. 初始化前后端工程目录（`frontend/` 与 `backend/`）
2. 先打通 `joinGarden -> petAction -> petStateDelta` 的最小链路
3. 实现离线摘要面板与服务器离线差异计算

---

## 2026-03-21（后端规格）

### 今日完成
1. 新增后端架构规格：`doc/后端开发设计文档.md`（FastAPI、MySQL、可选 Redis、DashScope/LangChain、JWT、模块与周期映射）
2. 新增前后端对接文档：`doc/API-后端与前端对接.md`（JWT access/refresh、REST 示例、WebSocket 消息与 `actionType` 枚举统一为 `Pat` 等）
3. 根目录增加 `.env.example`（占位符，不含真实密钥）
4. 同步栈说明：`README.md`、`doc/项目目录说明.md`、`AGENTS.md`；产品文档中摸头动作与 API 枚举对齐（`Pat`）
5. 修订：`doc/项目设计文档.md` MVP 动作列表第三条改为协议一致的 `Pat` 表述

### 已知风险 / 注意
- 实现时需敲定 `/api/v1` 前缀是否启用及 WebSocket 路径，与本文档保持一致即可。

### 下一步建议
- 周期 0：初始化 `backend/`，按对接文档实现 `/health`、认证骨架与 DB 连接。

---

## 2026-03-21（续）

### 今日完成
1. 将分周期开发计划落盘为独立文档：`doc/开发周期计划.md`
   - 周期 0～5 的目标、主要工作、验收标准与时间盒参考
   - 补充「团队协作与角色」：UI 设计组、前端组、后端组、测试组、运维组职责与和周期的对应关系
2. 明确阶段切换：由「产品设计文档齐备」进入**开发设计阶段**（工程实现与多组协作前的设计与对齐阶段）
3. 更新仓库导航：`README.md`、`doc/项目目录说明.md` 增加对 `开发周期计划.md` 的引用

### 当前状态
- 迭代路线图已固定，可作为各组排期与验收依据
- 下一步工作重心：**开发设计阶段**（协议/接口/视觉与工程目录对齐）后进入周期 0 编码

### 下一步建议（各组）
1. **UI 设计组**：对照 `assets/ui/` 与 [项目设计文档.md](项目设计文档.md)，输出开发用设计规范与切图清单（若需新增页面再补 mockup）
2. **前端组 / 后端组**：按 [切片0-2工程骨架.md](切片0-2工程骨架.md) 与 [开发周期计划.md](开发周期计划.md) 周期 0 启动工程骨架与协议草案
3. **测试组**：将各周期「验收」条目整理为可执行用例表（可从周期 1 开始）
4. **运维组**：准备本地/测试环境约定（Python/Node 版本、环境变量模板、后续 `infra/` 占位）

---

## 2026-03-21（运维：Git 远端）

### 今日完成
1. 本地初始化 Git 仓库，默认分支 `main`；首笔提交包含当前文档与 `assets/ui/` 等（提交信息见 `git log`）。
2. 关联远端并完成 **首次推送**：`origin` → SSH `git@github.com:hujiahua6623-dotcom/cute_cat.git`（用户在 GitHub 添加本机 SSH 公钥后，`git push -u origin main` 成功）。
3. 新增运维留痕：`doc/运维记录-仓库与首次推送.md`；更新 `doc/项目目录说明.md` 文档索引；推送后已同步运维记录正文（验收状态、审计说明）。

### 当前状态
- 远端 `main` 已与本地对齐；运维记录定稿提交见 `git log -1`（如 `8b60ed0` 起）。

### 下一步建议（运维）
1. 在 GitHub 上确认协作者权限与（按需）分支保护。
2. 按需补充 Issue/PR 模板与后续 `infra/` 或部署文档。
